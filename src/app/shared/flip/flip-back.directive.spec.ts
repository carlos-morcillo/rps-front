import { FlipBackDirective } from './flip-back.directive';

describe('FlipBackDirective', () => {
  it('should create an instance', () => {
    const directive = new FlipBackDirective();
    expect(directive).toBeTruthy();
  });
});
